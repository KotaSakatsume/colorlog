## 概要
Expo テンプレート由来の既存ファイルに残っていた `tsc --noEmit` エラー **19件を0に**解消する保守 PR。挙動は変えず型整合のみ（修正対象の `app-tabs*` はどこからも import されていない孤立テンプレで、ランタイム影響ゼロ）。

Closes #2

## パターン別の修正
| | 内容 | 件数 |
|---|---|---|
| A | `ColorSchemeName` の正規化を共通ヘルパ `useThemeScheme()` に集約。死んだ `=== 'unspecified'` 比較を撤去し、`null`/`undefined` を `'light'` に倒す（旧式の `Colors[null]=undefined` 潜在バグも解消） | 9 |
| B | SDK54 の NativeTabs 正しい API に追従（`Label`/`Icon` を `expo-router/unstable-native-tabs` の名前付き export に）。`Icon` に存在しない `renderingMode` を削除 | 6 |
| C | `SymbolView.name` をオブジェクトからプレーン文字列の SF Symbol 名に | 2 |
| D | 型付きルート `/explore`（不在）→ 実在する `/profile`。trigger 名とボタンラベルも整合 | 1 |
| E | `StyleSheet.absoluteFill`（数値ID・スプレッド不可）→ `absoluteFillObject` | 1 |

## 変更ファイル（7）
- `src/hooks/use-color-scheme.ts` / `use-color-scheme.web.ts` — `useThemeScheme()` を**両方に**追加（web は別実装のため必須）
- `src/hooks/use-theme.ts` — ヘルパ経由に
- `src/components/app-tabs.tsx`（孤立）— A + B
- `src/components/app-tabs.web.tsx`（孤立）— A + C + D
- `src/components/ui/collapsible.tsx` — C
- `src/components/animated-icon.tsx` — E

## 注記（設計の誤り訂正）
設計書 `01-design.md` の B 記述「`renderingMode` はそのまま流用可」は SDK54 の `IconProps` に当該プロパティが存在せず**誤り**。本PRでは正しく削除して吸収。孤立テンプレのため iOS の template tint 挙動への実害はなし。

## 品質ゲート
- `npx tsc --noEmit`: **0 errors**（19→0、新規エラーなし）
- `npx jest`: **3 suites / 26 tests pass**（型修正のみ、対象ファイルは jest 参照0・snapshot 0 で独立）

## やらないこと
孤立テンプレ（`app-tabs*`）の削除・大規模リライト、新ルート追加/typedRoutes 変更、配色・アーキテクチャ変更は行わない。
