## 概要
SPEC §2 / §5-3。認証「匿名開始→作成/参加時に Apple リンク」の **UI 導線を Mock 基盤で配線**する。native Apple 認証＋Firebase `linkWithCredential` は interface の継ぎ目のみ（実呼び出しは Firebase 実装 Issue）。

Closes #12

実 SDK 54。Mock＋node jest で完結検証。画面は `useCurrentUser`/`useRepositories().auth` 経由で、`expo-apple-authentication` を import しない。

## 変更
- `src/repositories/types.ts`: `AuthUser.isAnonymous: boolean`（必須）＋ `AuthService.linkWithApple(): Promise<AuthUser>`
- `src/repositories/mock/mock-auth-service.ts`: `MOCK_CURRENT_USER.isAnonymous: true`、`linkWithApple()` simulate（匿名→連携、displayName は既定名 'あなた' のときだけ 'Apple ユーザー' に更新・編集済みは維持、1回通知、連携済みは冪等 no-op）
- `src/app/(tabs)/profile.tsx`: アカウント状態（匿名/連携済み）表示＋「Apple と連携」ボタン
- `src/app/trip/create.tsx` / `join.tsx`: 匿名時のみ非ブロッキング CTA「Apple と連携してアルバムを守る」（作成/参加の disabled には非連動・連携済みは非表示）
- AuthUser 必須化の追従: `mock-post-repository.test.ts`/`mock-upload-queue.test.ts` の AuthUser リテラル、`mock-upload-queue.ts:198` の `job.user→AuthUser` 変換境界で `isAnonymous:false` 補完（永続化構造型 `UploadJob.user` は不変・promote 挙動不変）
- 新規 `src/repositories/mock/mock-auth-service.test.ts`（7ケース）

## 品質ゲート
- `npx tsc --noEmit`: **0 errors**
- `npx jest`: **8 suites / 79 tests pass**（既存72 + 追加7）
- `expo-apple-authentication` は画面/Mock/domain に未 import（native 隔離・jest 非破壊）

## やらないこと（別Issue・Firebase実装層）
実 Apple 認証（`signInAsync`）と Firebase `linkWithCredential`、サインアウト/アカウント削除、作成/参加の連携必須化。

## 軽微 follow-up（非ブロック）
upload-queue の将来 AuthUser 拡張 TODO コメント、CTA の文言トーン統一（レビュー nit）。
