## 概要
SPEC §9-7前半 / §13.3。ベスト9昇格時の画像2サイズ生成の土台として、**サイズ決定の純ロジック＋`ImageProcessor` 抽象（Mock＋expo-image-manipulator 実装）**を Mock＋node jest で完結検証できる形で追加する。実 Storage アップロード・promote 配線は別Issue（§9-7後半）＝**継ぎ目のみ**。

Closes #10

実 SDK 54。本画像長辺は **1600px**（§13.3 コスト規律を正とする。§5-5 の2048は本Issueでは文面修正せず）。

## 追加
- `src/domain/image-sizing.ts`: `computeTargetSize(srcW,srcH,maxLongEdge)`（アスペクト比保持・拡大しない・長辺がmaxを超えない・0/負/NaN/undefined を安全に）＋定数 `MAIN_MAX_LONG_EDGE=1600` / `THUMB_MAX_LONG_EDGE=400` / `JPEG_QUALITY=0.7`
- `src/repositories/types.ts`: `ProcessedImage` / `ProcessedImages` / `ImageProcessor` ＋ `Repositories` 束に `imageProcessor`
- `src/repositories/mock/mock-image-processor.ts`: expo 非依存。`computeTargetSize` で寸法算出・uri スタブ。寸法欠落（compose は `{uri}` のみ＝常態）をフォールバック
- `src/repositories/expo/expo-image-processor.ts`: v14 新 Context API（`manipulate(uri).resize(片辺).renderAsync()` → `saveAsync({compress:0.7, format:SaveFormat.JPEG})`）で本画像1600＋サムネ400。**native のため node テスト対象外**（型のみ）。旧 deprecated `manipulateAsync` 不使用
- `src/repositories/mock/index.ts`: `MockImageProcessor` 注入。`context.tsx` は無改修（Mock 経由・expo を static import しない＝node テストで落とさない）

## 品質ゲート
- `npx tsc --noEmit`: **0 errors**（expo 実装含め型クリーン）
- `npx jest`: **7 suites / 72 tests pass**（既存51 + 追加21）
- `jest --listTests` に expo 実装は非混入（native 混入なし）

## やらないこと（別Issue）
実 Storage/Firebase アップロード、UploadQueue/promote への実配線、expo-camera 実カメラ統合。

## 軽微 follow-up（非ブロック）
expo 実装のフォールバック・コメント位置の明確化、Mock フォールバック寸法の assert 追加（レビュー should/nit）。
