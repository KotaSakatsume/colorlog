## 概要
`COLOR_POOL` を SPEC §6 準拠の**12色**に確定し、`MAX_MEMBERS`（=色プール数）を実行時 8→**12** にする。セキュリティルール(Issue #6, メンバー上限12強制)とアプリ側を整合。配布アルゴリズムは不変（純データ拡張＋テスト整合）。

Closes #8

## 確定12色（明度差で色覚多様性に配慮）
あか#E63946 / だいだい#F3722C / きいろ#FFD23F / きみどり#A7C957 / みどり#2A9D4A / あおみどり#1D9A8D / みずいろ#4CC9F0 / あお#1D6FE0 / あいいろ#3F3D9E / むらさき#8E44AD / もも#F072B6 / ちゃいろ#8B5E34

近接3群を明度差で分離（黄系: きいろ207/だいだい145 を採用し中間の やまぶき を脱落 / 青系: みずいろ168/あお99/あいいろ73 を階段配置）。最高明度を二重占有する しろ#FFFFFF は脱落（12枠を有彩色＋茶に分散）。色相が混同する色覚下でも明度＋日本語名で判別可能。

## 変更
- `src/domain/colors.ts`: `COLOR_POOL` を全有効な12要素配列に書き直し（コメントアウト運用を撤廃）。`MAX_MEMBERS` は導出のまま自動で12。
- `src/domain/assign-colors.test.ts`: `COLOR_POOL.length===12`・hex/名前重複なしガード追加、12人配布（Set.size===12）・13人目で TooManyMembersError の literal アサート追加（タイトルと実数のズレ解消）。
- `assign-colors.ts` は無変更（ロジック不変、コメントは既に整合）。

## 波及（変更不要・確認済み）
seed.ts は しろ 未参照で安全 / UI は flexWrap で色数非依存 / `COLOR_POOL[0]` 参照テストは値非依存 / `contrastTextColor` は最暗 あいいろ・最明 きいろ とも妥当。

## 品質ゲート
- `npx tsc --noEmit`: **0 errors**
- `npx jest`: **5 suites / 51 tests pass**（既存49 + 追加2）

## やらないこと
配布アルゴリズム変更、UI配色刷新、セキュリティルール変更（既に12）。`BLUE=COLOR_POOL[0]` のリネームはスコープ外（値非依存・据え置き）。
